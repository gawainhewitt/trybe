import { StyleSheet } from "react-native";

export default StyleSheet.create({
  goal: {
    padding: 20,
    borderWidth: 1,
    borderColor: "#bbb",
    borderRadius: 5,
    marginBottom: 12,
  },
  wrapper: {
    padding: 20,
  },
});
